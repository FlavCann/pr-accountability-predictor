# Adjudication cases

One JSON file per case, labelled by a person. Schema and rules:
`src/pr_predictor/evals/adjudication.py` and `evals/GUIDELINES.md`.

Empty until an analyst writes cases. Verdict labels are a judgement about a
named company, so they are never generated.
